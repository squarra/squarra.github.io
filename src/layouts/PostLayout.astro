---
import Layout from "./Layout.astro";
const { frontmatter } = Astro.props;
---

<Layout>
    <time datetime={frontmatter.pubDate.toISOString()}>
        {frontmatter.pubDate.toDateString().split(' ').slice(1).join(' ')}
    </time>
    <slot />
</Layout>